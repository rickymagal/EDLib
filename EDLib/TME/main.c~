#include "TME.h"
#include <stdio.h>
#include <stdlib.h>

void print_float(data_type data)
{
    printf("%.6f", data);
}

int main(){
  TME *t = TME_create(2,2);
  TME *l = TME_create(2,2);
  Position position22;
  position22.line =2;
  position22.column=2;
  Position position12;
  position12.line =1;
  position12.column =2;
  Position position11;
  position11.line = 1;
  position11.column = 1;
  Position position21;
  position21.line = 2;
  position21.column = 1;
  TME_insert(&t, 2, position11);
  TME_insert(&t, 2, position22);
  TME_print_dense(t, print_float);
  t = TME_multiply_scalar(t, 2);
  TME_print_dense(t, print_float);
  TME_destroy(t);
}
